# ============================================
# TA Platform — Demo Mode Configuration
# ============================================
# For demo: only DEEPSEEK_API_KEY is required.
# Database is SQLite (auto-created), Redis is disabled.
# Everything else works out of the box.

# ★ DeepSeek API Key — REQUIRED ★
# Get your key at: https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-7845ccdc5eba41bcbb5bd60d61238a86

# Database (SQLite by default, no setup needed)
DATABASE_URL=sqlite+aiosqlite:///./ta_platform.db

# JWT secret (change for production)
JWT_SECRET=demo-secret-change-in-production

# SMS verification codes are printed in the console in demo mode
